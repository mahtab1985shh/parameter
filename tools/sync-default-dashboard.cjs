const fs=require('node:fs'),path=require('node:path'),vm=require('node:vm');const file=path.join(__dirname,'..','parameter-v34-login-sharp.html');let h=fs.readFileSync(file,'utf8'),m=h.match(/var binary=atob\("([^"]+)"\)/);let p=Buffer.from(m[1],'base64').toString('utf8');const js=fs.readFileSync(path.join(__dirname,'default-dashboard.js'),'utf8'),css=fs.readFileSync(path.join(__dirname,'default-dashboard.css'),'utf8');new vm.Script(js);const enc=s=>JSON.stringify(s).slice(1,-1);let marker=p.indexOf('function persianToday(){'),start=p.lastIndexOf('(function(){',marker),end=p.indexOf('function userUtilityAction(type){',marker);if(marker<0||start<0||end<0)throw Error('Dashboard JS boundaries missing');p=p.slice(0,start)+enc(js+'\n')+p.slice(end);let cs=p.indexOf('/* Default dashboard only —'),ce=p.indexOf('.parameter-date-overlay{',cs);if(cs<0||ce<0)throw Error('Dashboard CSS boundaries missing');p=p.slice(0,cs)+enc(css+'\n')+p.slice(ce);h=h.replace(m[1],Buffer.from(p).toString('base64'));fs.writeFileSync(file,h);console.log('Default dashboard synced');
