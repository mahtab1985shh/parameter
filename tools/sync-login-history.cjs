const fs=require('fs'),path=require('path'),vm=require('vm'),assert=require('assert');const file=path.join(__dirname,'..','parameter-v34-login-sharp.html');let h=fs.readFileSync(file,'utf8');const m=h.match(/var binary=atob\("([^"]+)"\)/);let p=Buffer.from(m[1],'base64').toString(),enc=s=>JSON.stringify(s).slice(1,-1);const marker=p.indexOf("var selected='success',root"),a=p.lastIndexOf('(function(){',marker),b=p.indexOf('(function(){',marker);assert(a>=0&&b>a);const js=fs.readFileSync(path.join(__dirname,'login-history.js'),'utf8');new vm.Script(js);p=p.slice(0,a)+enc(js+'\n')+p.slice(b);const ca=p.indexOf('.lh-root{'),cb=p.indexOf('.uc-root{',ca);assert(ca>=0&&cb>ca);p=p.slice(0,ca)+enc(fs.readFileSync(path.join(__dirname,'login-history.css'),'utf8')+'\n')+p.slice(cb);h=h.replace(m[1],Buffer.from(p).toString('base64'));fs.writeFileSync(file,h);
