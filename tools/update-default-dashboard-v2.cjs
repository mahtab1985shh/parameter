const fs=require('node:fs'),path=require('node:path'),vm=require('node:vm'),file=path.join(__dirname,'..','parameter-v34-login-sharp.html');let h=fs.readFileSync(file,'utf8'),m=h.match(/var binary=atob\("([^"]+)"\)/);if(!m)throw Error('payload missing');let p=Buffer.from(m[1],'base64').toString('utf8');const map=fs.readFileSync(path.join(__dirname,'iran-map-data.js'),'utf8'),js=fs.readFileSync(path.join(__dirname,'default-dashboard-v2.js'),'utf8'),css=fs.readFileSync(path.join(__dirname,'default-dashboard-v2.css'),'utf8');new vm.Script(map+'\n'+js);const enc=s=>JSON.stringify(s).slice(1,-1),sm='function userUtilityAction(type){';if(p.split(sm).length!==2)throw Error('script marker');p=p.replace(sm,()=>enc(map+'\n'+js+'\n')+sm);const cm='.parameter-date-overlay{',i=p.indexOf(cm);if(i<0)throw Error('css marker');p=p.slice(0,i)+enc(css+'\n')+p.slice(i);h=h.replace(m[1],Buffer.from(p).toString('base64'));fs.writeFileSync(file,h);console.log('Compact provincial dashboard integrated');
